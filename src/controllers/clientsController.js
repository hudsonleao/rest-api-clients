

module.exports = {
    getClients: ({ models: { clientsModels } }) => async () => {

    },

    getClientsById: ({ models: { clientsModels } }) => async () => {

    },

    getClientsByParams: ({ models: { clientsModels } }) => async () => {

    },

    createClients: ({ models: { clientsModels } }) => async () => {

    },

    updateClients: ({ models: { clientsModels } }) => async () => {

    },

    deleteClients: ({ models: { clientsModels } }) => async () => {

    }

}